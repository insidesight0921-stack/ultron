# 다음 작업 — 페이퍼 성과 → Obsidian 회고 루프

> 작성일: 2026-08-26 · 2단계 코드 완료, 3단계 초안 생성 완료 / 운영 연결 대기
> 대상: `scripts/paper_metrics.py`(기존 계산 계층 재사용), 신규 리포터 스크립트, `obsidian-vault/wiki/투자/성과/`

## 1. 현재 상태

`paper.db` → vault 경로는 `paper_weekly_report.py`로 구현됐지만 launchd 등록과 실제 운영
노트 확인은 남아 있다. 원칙 제안은 `principle_review.py`가 Private 상태 디렉터리에 승인용
초안만 만들며, 승인 전에는 vault를 쓰지 않는다.

| 스크립트 | 쓰는 내용 |
|---|---|
| `inbox.py` / `inbox_bot.py` | 텔레그램 메모·URL → `raw/inbox/` |
| `refine_raw.py` | raw → Gemma 정제 → `wiki/` |
| `learning_bot.py` | 제안 노트 (경로 수동 지정) |

설계도(`local-ai-agent-plan.md` 6장)의 회고 흐름 중 주간 기록과 원칙 제안 초안 생성은
구현됐다. 남은 경계는 실제 주간 노트 검증, 월간 실행 일정, Telegram 승인 UX와 승인된
변경만 wiki에 반영하는 적용기다.

데이터 계층상 vault의 `wiki`·`raw`는 이미 **개인 데이터**(local Gemma 전용, MCP 미노출,
`docs/DATA_CLASSIFICATION.md` 39행)이므로 성과를 여기 적어도 경계는 깨지지 않는다.

## 2. 1단계 — 원칙 노트 정합 (완료)

- [x] `wiki/투자/퀀트/마이퀀트_진입조건.md` 신설 (2026-08-26)
      코드에만 있던 진입 조건 4종·대조군 3종·청산 규칙을 노트화. 단일 진실은 `entry_backtest.py`
- [ ] `index_wiki.py` 실행해 RAG 반영 (Wiki 24 → 25개)
- [ ] IPO 배점 정합은 `다음작업_IPO_배점개편.md` 참고

## 3. 2단계 — 주간 성과 스냅샷 자동 기록  ⏳ 코드 완료 / 스케줄 등록 대기

**2026-08-26 구현분**: `scripts/paper_weekly_report.py` + `tests/test_paper_weekly_report.py`(25개).
순수 계산·렌더는 완료했고 남은 것은 launchd 등록뿐이다. 수동 실행은 지금도 가능하다.

```
.venv/bin/python scripts/paper_weekly_report.py --stdout        # 노트 미리보기(파일 안 씀)
.venv/bin/python scripts/paper_weekly_report.py                 # wiki/투자/성과/에 저장
.venv/bin/python scripts/paper_weekly_report.py --date 2026-08-19  # 지난 주차 재생성
```


매주 금요일 장 마감 후, `paper_metrics.py`(순수 FIFO 계산 계층, 이미 있음)로 집계해
노트 한 장을 **결정론적으로** 생성한다.

**Gemma 정제를 태우지 않는다.** 숫자를 LLM에 통과시키면 왜곡 위험이 있다. raw→wiki
파이프라인을 쓰지 말고 `wiki/투자/성과/주간_YYYY-Www.md`에 직접 쓴다.
`index_wiki.py`가 `wiki/**`를 인덱싱하므로 RAG는 자동으로 참조하게 된다.

### 노트 형식

```markdown
---
type: paper-weekly
period: 2026-W35
판단시점: 2026-08-21
평가시점: 2026-08-28
---
# 페이퍼 주간 성과 2026-W35

| 슬롯 | 실현손익 | 승률 | 표본 |
|---|---:|---:|---:|
| 콴텍 | +182,400 | 61% | 18 |
| 키움 | −44,100 | 47% | 32 |

## 조건별 (마이퀀트)
| 태그 | 승률 | 손익비 | 표본 |
|---|---:|---:|---:|
| 추세위+눌림 | 58% | 1.4 | 12 |
| 신고가돌파 | 41% | 0.9 | 7† |

† 표본 5건 미만, 참고용

## 적용 원칙
- [[모멘텀_전략_원칙]]
- [[마이퀀트_진입조건]]
```

핵심은 **백링크**다. `[[원칙노트]]`를 걸어 두면 옵시디언 그래프에서 "이 원칙이 어떤 성과를
냈는지"가 역방향으로 조회된다.

### 작업 항목

- [x] 신규 스크립트 `paper_weekly_report.py` — 순수 집계 + 노트 렌더 + I-O 경계 분리
- [x] 슬롯별·마이퀀트 태그별 집계 (`trade_analytics.compute_roundtrips` 재사용, 지연 임포트)
- [x] frontmatter에 **판단시점·평가시점 분리 기록** (look-ahead 차단)
- [x] 라운드트립은 **청산일 기준** 귀속 — 미청산 포지션의 평가손익은 집계하지 않음
- [x] 표본 5건 미만 라벨 † 표기
- [x] 같은 주차 재실행 시 덮어쓰기(멱등), 원자적 쓰기(임시 파일 → os.replace)
- [x] 거래 없는 슬롯은 표에서 제외하고 이름만 별도 표기
- [x] 단위 테스트 25개 (기간 귀속·집계·태그·백링크·렌더·원자적 쓰기)
- [x] launchd 잡을 `agent_services.sh`에 추가 — `com.hyunjun.ai-agent.paper-weekly-report`,
      매주 금 16:30 (장 마감 15:30 이후, collector 16:20 뒤). install/start/stop/status
      모든 경로에 연결했고 `bash -n` 문법 검사와 plist XML 파싱을 확인했다
- [ ] **맥에서 `bash scripts/agent_services.sh install` 실행** — 원격 세션은
      `~/Library/LaunchAgents`에 접근할 수 없어 등록만 사용자가 실행해야 한다
- [ ] 실제 paper.db로 1회 실행해 노트 확인

## 4. 3단계 — 원칙 갱신 제안 (HITL)

월 1회, 표본 기준을 넘긴 태그에 한해 "강화/수정/폐기" **초안만** 만들어 텔레그램 승인을 받고,
승인 시에만 wiki 노트를 갱신한다. **원칙 자동 덮어쓰기 금지.**

- [x] 제안 생성 — `principle_review.py`, 원칙별 승률·손익비·표본 수 기반, 반례·최소표본
      가드와 Private 초안 저장, 25개 단위 테스트
- [ ] 텔레그램 승인 UX 재사용 (`[✅ 반영] [✏️ 수정 후 반영] [❌ 보류]`)
- [ ] 승인된 변경만 wiki 반영, 변경 이력 기록

## 5. 반드시 지킬 가드 (설계도 6장과 동일)

| 편향 | 방어 |
|---|---|
| look-ahead | 판단시점·평가시점 분리 저장. 평가는 판단시점 정보로만 |
| 과적합 | 최소 표본 미달 시 제안 보류 |
| 확증 편향 | 제안 프롬프트에 "반례 적극 탐색" 명시 |
| 생존 편향 | 실패 사례도 동등 기록 |
| 원칙 오염 | 수정은 항상 사람 승인(HITL) 게이트 경유 |

## 6. 순서

2단계가 3단계의 입력이다. **2단계 없이 3단계를 먼저 하지 말 것.**
1단계(원칙 노트 정합)가 안 되면 백링크 대상이 없으므로 1 → 2 → 3 순서를 지킨다.
