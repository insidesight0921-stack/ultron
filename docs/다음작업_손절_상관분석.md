# 🚀 다음 작업 — 손절 실행 지연 수정 + 손실 상관 분석

> 새 세션은 이 파일을 먼저 읽으세요. trade_analytics 정밀 진단(v1.1)에서 도출된 두 후속 작업의 설계·결정사항을 정리했습니다.
> 작성: 2026-07-08 세션 / 대상 레포: `~/울트론/ai-agent`

## 0. 배경 (정밀 진단 실측 근거)
- 손절 30건 **평균 -14.2%** (손절선 -7%인데 30건 전부 손절선보다 깊게, 19건은 5%p+ 초과) → **손절선이 아니라 실행이 늦다.**
- 휩쏘(손절 후 14일내 재매수) 4건, 손실 집중 워스트3 = 총손실의 42%.
- 월별 실현손익: 5월 +598만 → 6월 -555만 → 7월 -656만 (연속 악화).
- 조회: 텔레그램 "성과 분석해줘" → trade_analytics.deep_report (🔎 정밀 진단 섹션).

---

## A. 손절 실행 지연 손보기

### 현재 구현
- `scripts/telegram_bot.py` `intraday_monitor_job` (≈line 1053).
- 등록: `interval=INTRADAY_MONITOR_INTERVAL_SEC=30*60`(30분), `first=180`, 평일 09:05~15:30.
- 손절 `INTRADAY_STOP_LOSS_PCT=-0.07`, 익절 `INTRADAY_TAKE_PROFIT_PCT=+0.20`.
- 가격 원천 `_get_pykrx_price()` = pykrx OHLCV **최신 종가**(장중 실시간 아님).

### 근본 원인 (2중 지연)
1. **30분 폴링** — 급락/갭에서 -7% 돌파를 늦게 감지.
2. **종가 기반** — pykrx 최신 종가라 장중 실시간 반영이 늦음.
→ 둘이 겹쳐 평균 -14.2%에서야 청산.

### 옵션 (결정 필요)
1. **검사 간격 단축** (30분→5분/1분): 간단·부하↑. 종가 지연은 잔존.
2. **장중 실시간 시세 원천 도입** (pykrx 종가→실시간): 근본적. 데이터소스·검증 필요.
3. **갭/급락 즉시 청산 로직** or 단계·트레일링 손절.
4. **손절선(-7%) 재검토**: 데이터상 폭이 아니라 실행이 문제라 후순위.

### 파일 / 테스트
- 수정: `telegram_bot.py` 상수 + `intraday_monitor_job`.
- **순수 분리 권장**: `should_exit(pnl_pct, stop, take) -> "손절"|"익절"|None` 순수함수로 빼서 hermetic 테스트(샌드박스는 텔레그램/pykrx 불가).
- 실환경: 장중 실측 필요(간격 단축·시세원 정상 여부).

### 결정 질문
- (a) 간격 단축만? (b) 실시간 시세원 도입까지? (c) 손절선 조정 포함?

---

## B. 손실 ↔ 거시국면 / 섹터 상관 분석

### 목표
6~7월 손실이 특정 **거시 국면**이나 **섹터**에 몰렸는지 정량화 → 회피/비중 규칙 후보 도출.

### 데이터 소스
- **거시 국면**: `quant_bot.snapshot(months).consensus_phase` ∈ {Recovery, Expansion, Slowdown, Contraction}.
  - 주의: snapshot은 *현재* 국면. 과거 월 국면은 (i) 해당 시점 기준 재계산 or (ii) 근사값 라벨링 필요(결정).
- **섹터**: 현재 종목→섹터 매핑 **없음**. 후보: pykrx 업종분류 vs 보유 종목 수동 맵(15종목 규모라 수동도 현실적).

### 설계 (순수 우선)
- `trade_analytics` 확장: `correlate_pnl_by(rts, label_fn) -> {label: {pnl, n, win_rate}}`.
  - `label_fn(roundtrip) -> 라벨`(국면 or 섹터)을 **주입**해 순수·테스트 가능.
- 리포트/LLM 총평에 "손실이 X 국면·Y 섹터에 N% 집중" 한 줄 추가.

### 파일 / 테스트
- `scripts/trade_analytics.py`(순수 상관 함수 + 포맷), 라우팅은 기존 `analysis` 토픽 재사용.
- 라벨 소스(phase_fn/sector_fn)만 주입식이면 hermetic 테스트 가능.

### 결정 질문
- 섹터 분류: pykrx 업종 vs 수동 맵? / 국면: 월별 재계산 vs 현재값 근사?

---

## 착수 순서(권장)
1. **B의 순수 상관 함수**(label_fn 주입)부터 — 샌드박스에서 바로 테스트 가능, 위험 없음.
2. **A는 `should_exit` 순수 분리 + 옵션 결정** 후 실환경(장중) 검증.

## 참고 (현 상태)
- trade_analytics v1.1 · paper_analytics(성과·스냅샷) · strategy_feedback(제안·승인형 골격) 완료.
- hermetic 회귀 759 PASS. 라우팅: "성과 분석/매매 습관/손익비" → analysis, "비중 제안" → feedback, "내 성과" → performance.

---

## 진행 상태 (2026-07-08 세션 갱신)
- ✅ **B 완료** (trade_analytics v1.2): `correlate_pnl_by(rts, label_fn)` 순수 함수 + `sector_label`(수동 맵 `SECTOR_BY_TICKER`, 14종목) + `phase_label`(월별 수동 근사 맵 `PHASE_BY_MONTH`: 5월 Expansion, 6~7월 Slowdown).
  - `format_correlation` 섹션이 deep_report에, 국면·섹터별 손익이 LLM 프롬프트에 포함됨.
  - 실측: Slowdown 국면에 손실 97% 집중, 섹터는 전력설비 22% > 건설 > 화학 순 분산.
  - 신규 종목/새 달은 두 맵 상수에 수동 추가. 테스트 11개 추가(전체 913 PASS).
- ⚠️ 사전 존재 실패 1건: `test_router_prompt_size_reasonable` (프롬프트 12,927자 > 12,000 한도). B와 무관(라우터 프롬프트는 정적 템플릿). 한도 갱신 or 프롬프트 축약 필요.
- ⏭️ **A 다음**: 결정됨 — 4옵션 전부(5분 간격 + 실시간 시세원 + 갭/급락 즉시청산·트레일링 + 손절선 조정). `should_exit` 순수 분리부터.
- ✅ **A 완료** (v3.44): 판정 로직을 `scripts/exit_rules.py` 순수 모듈로 분리(`should_exit`).
  - ① 폴링 30분→**5분**(`INTRADAY_MONITOR_INTERVAL_SEC` env 조정 가능) ② 시세원: **네이버 polling API 실시간 우선, pykrx 종가 폴백**(`_get_realtime_price`) ③ **급락 하드스톱 -12%**(갭 포함, 사유 "급락") + **트레일링**(+10% 도달 후 7%p 반납 청산, 피크는 `data/cache/intraday_peaks.json` 영속·물타기/재진입 시 자동 리셋) ④ 손절선 -7% 유지하되 `EXIT_STOP_PCT` 등 env로 조정 가능 — 실행 지연 수정 후 슬리피지 재측정하여 결정.
  - 청산 notes에 사유 포함(`손절 자동청산[급락] (-13.2%)`) → trade_analytics 사유 분류 호환.
  - 테스트 17개 추가(전체 930 PASS). **⚠️ 실환경 검증 필요**: 장중 네이버 API 응답(샌드박스 네트워크 차단으로 미실측 — 실패 시 pykrx 폴백으로 안전), 5분 간격 부하, 트레일링 오작동 여부 로그 확인.

## 보완 라운드 (2026-07-09 세션)
1. ✅ 오호가 방어 — `exit_rules.confirm_price`: 직전 폴링가 대비 15%+ 급변 시 pykrx 교차확인, 불일치면 해당 사이클 스킵. 상태 파일 `intraday_peaks.json` → `{peaks, px}` 구조 확장(구형 자동 마이그레이션).
2. ✅ 시세 병렬 조회 — `asyncio.gather`, 네이버 타임아웃 5→3초.
3. ✅ 트레일링 백테스트 — `scripts/exit_backtest.py`(순수 simulate_exit/grid_search + 합성 경로 테스트 10개). ⚠️ 캐시 OHLCV는 합성 더미라 실측 튜닝은 실환경에서 `python3 scripts/exit_backtest.py` 실행(pykrx 필요). 결과 보고 arm/drop 기본값(+10%/7%p) 조정.
4. ✅ 슬리피지 전/후 비교 — `slippage_comparison`(cutoff=2026-07-09). 수정 후 손절 표본이 쌓이면 정밀 진단에 "전/후" 줄 자동 표시 → 손절선 조정 판단 근거.
5. ✅ 소표본 경고 — 라벨 n<5 → † + 각주.
6. ✅ 국면 자동 축적 — quant snapshot 성공 시 `record_phase_month` → `data/cache/phase_by_month.json`. `merged_phase_map()`이 실측 우선 병합, deep_report에 적용(수동 근사는 폴백).
7. ✅ 미분류 노출 — 섹터맵에 없는 종목 리포트에 경고 표시.
- 전체 954 PASS (신규 24개). 잔여: 라우터 프롬프트 12,927자(기존 red 1건), **git 미추적 — scripts/·tests/·docs/ 전체가 untracked, 커밋 필요**, `scripts/.write_test`·`.test_write` 잔여 파일 삭제.

## 엣지 발굴 (2026-07-09 세션, trade_analytics v1.4)
- ✅ 텔레그램 "엣지 분석해줘" → edge 토픽(router/system_info) → `trade_analytics.edge_report()`.
- 컷: 보유기간·매수요일·재진입·일괄진입. 규칙 후보 `EDGE_RULES`(R1 손절후 30일 재진입 금지, R2 Slowdown 진입 축소) — since=2026-07-09 이후 매수분으로 **전방 검증**, 리포트에 전방/과거 분리 표시.
- 기각: '목요일 자제'(교란 — 6/18·7/2 일괄매수), '일괄진입 자제'(42건 중 40건이라 판별력 없음).
- 상세: `docs/엣지분석_2026-07-09.md`. 전방 표본 위반 n≥10에서 열세 지속 시 strategy_feedback 제안으로 승격.
- 테스트 964 PASS. 커밋 필요(라우터·system_info·trade_analytics·docs 변경분).

## 시스템 검토 보완 (2026-07-09 세션 2차, v3.46)
1. ✅ 시세원 건강 감시 — `exit_rules.naver_health`: 네이버 전멸 연속 3사이클(EXIT_NAVER_WARN_STREAK) 도달 순간 텔레그램 1회 경고(pykrx 폴백 동작 중 = 실행 지연 재발 위험 알림). 상태 json에 naver_streak 영속.
2. ✅ 라우터 프롬프트 다이어트 — 12,927→11,285자(중복 예시 축약, 라우팅 테스트 전부 유지). red였던 test_router_prompt_size_reasonable 해소, **전체 970 PASS / 0 fail**.
3. ✅ KST 게이트 — `_now_kst()` 도입, intraday_monitor·technical_signal 장중 게이트가 서버 타임존 무관.
4. ✅ 백테스트 진입가 — compute_roundtrips에 qty 추가, exit_backtest가 실매수가(cost/qty) 사용.
- 잔여(후순위): 국면 기록 월1회 한계, 성과 표시용 가격 1곳 pykrx 종가, .envcd 정리, 트레일링 vs 익절선 경합은 실환경 `python3 scripts/exit_backtest.py` 결과로 판단.

## 진입 조건 발굴 (2026-07-09 세션 3차)
- ✅ `scripts/entry_backtest.py` — 청산 구조 고정 이벤트 스터디(순수 코어 + pykrx CLI).
  실행: `--tag`(42건 진입 태깅) / 기본(KOSPI200×3년 조건 그리드). 상세 docs/엣지분석 §6.
- 테스트 982 PASS. 다음 세션: 실환경 CLI 결과 → 조건 채택/기각 → EDGE_RULES 등록.

## 나만의 퀀트 탭 (2026-07-09 세션 4차, v3.47)
- ✅ paper UI에 "🧪 나만의 퀀트" 탭: entry_backtest 진입 조건(추세위+눌림/정배열/신고가돌파/과낙폭반등)으로 KOSPI200 현재 시점 스캔(당일 캐시) → 리스트에서 매수 승인.
- ✅ 매수 시 notes에 `MQ[태그]` 자동 기록 → compute_roundtrips가 buy_notes 보존 → `tag_performance`로 태그별 성과 축적(탭 하단 + 엣지 분석 리포트에 표시).
- ✅ 슬롯 "마이퀀트"(초기 1,000만, `paper_db.ensure_slot` 멱등 생성) — 청산은 공용 자동 손절/트레일링 그대로 적용, 국면·섹터 상관에도 자동 포함.
- API: GET /api/paper/myquant-scan(?refresh=true), GET /api/paper/myquant-tags.
- 신규 종목은 섹터 "미분류" 경고로 표시됨 → SECTOR_BY_TICKER에 추가.
- 테스트 991 PASS. 커밋 필요.
